syms a; % 符号
syms d;

% %===============================================
a = 0.3;
d=1.4;
D1=[0,1,0;0,0,1;-pi*0.5*c,-a,-b]; %系数矩阵,平衡点为0
D2=[0,1,0;0,0,1;pi*d,-a,-b]; %系数矩阵,平衡点为0
% D=[0,1,0;0,0,1;1,-a,-a]; %系数矩阵,平衡点为0
% D=[0,1,0;0,0,1;-pi*d,-a,-a]; %系数矩阵,平衡点为1,3,5
% % aa=sym(D);
% % bb=poly(aa);  %求特征多项式表达式
% % cc=simple(bb);%化简特征多项式
[V,A]=eig(D1);
 d1=diag(A)
 aa=sym(D);
 bb=poly(aa);
 cc=simple(bb)
%  [V,A]=eig(D2);
%  d2=diag(A)
%===============================================